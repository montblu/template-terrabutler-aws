region         = "${region}"
profile        = "${profile}"
key            = "${environment}-${site}.tfstate"
bucket         = "${organization}-${environment}-site-${site}-tfstate"
encrypt        = true
